#.. WAF to print the length of the list (list is the parameter)

cities= ["Mumbai","Gujarat","Delhi","Bihar","Chennai","Madhya Pradesh","Banglore","Tamil Nadu","Assam"]
heroes = ["Hanuman", "Angad", "Jamvan", "Akshay Kumar", "Bahubali", "Ravan", "Indrajit", "Vibhishan"]

# def print_len(list): # the list is our parameter in this case, which can have multiple list
#     print(len(list)) # this is built-in print function, which will calculate the length of an list. (List can be any of these two mentioned)

# print_len(cities) # We are passing the arguments cities is the arguments which we are passing as an parameter in the function call.
# print_len(heroes)









